# 1. 의존성 모듈 (제일 중요! 절대 올리면 안 됨)
node_modules/

# 2. 빌드 결과물 (코드 짜면 자동 생성되는 것들)
dist/
build/
out/

# 3. 환경변수 및 비밀번호 (해킹 방지! ⭐)
.env
.env.local
.env.*.local

# 4. 운영체제 잡동사니 (맥이나 윈도우가 몰래 만드는 파일)
.DS_Store
Thumbs.db

# 5. 에디터 설정 파일 (내 VS Code 설정이 친구한테 강요되면 안 되니까)
.vscode/
.idea/

# 6. 로그 파일 (에러 기록 등)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*